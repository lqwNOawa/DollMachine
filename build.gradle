plugins {
    id 'java'
    id "com.github.johnrengelman.shadow" version "6.1.0"
}

group 'com.luoqiwen'
version '1.0-SNAPSHOT'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {

}

shadowJar {
    baseName(rootProject.name)
    version(null)
    manifest {
        attributes(
                'Main-Class': 'com.luoqiwen.dollmachine.DollMachine'
        )
    }
}